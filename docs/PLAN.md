- 对外暴露真实执行版本的 swap_tokens，返回 tx_hash；同时在输入层强制校验：
- 必须包含 dex/router（或明确仅支持 Uniswap 并以输入 router_address 为必填）
- 必须包含 slippage
- 必须包含 max_spend 或 min_receive 之一
- 调整执行路径以满足“最多一次链上交易”的不变式：
- 可采用预授权策略，或将 approve 与 swap 合并为同一原子操作的约束设计；若无法避免，需与规范对齐或更新规范
- 注册并暴露 news_search 到服务器路由；补充 onchain_transfer_monitor、market_volume 的实现与路由
- 实现 equity_price（P2 可选）或在文档明确暂不支持
- 落实 MAX_GAS_LIMIT 的校验，限制交易 gas
- 增加规范要求的稳定性验收测试（压力测试脚本或集成测试用例）
- 文档与工具名统一，明确输入输出契约与失败语义